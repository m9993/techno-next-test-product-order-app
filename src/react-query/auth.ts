export const useLogin = () => {
  //   return useMutation({
  //     mutationFn: async (body: { loginId: string; password: string }) => {
  //       const loginResponse = await server.auth.login(body);
  //       if (loginResponse?.data?.accessToken) {
  //         try {
  //           await AsyncStorage.setItem('accessToken', loginResponse.data.accessToken);
  //         } catch (e) {
  //           console.log(e);
  //         }
  //       }
  //       return loginResponse;
  //     },
  //     mutationKey: 'login',
  //   });
};
